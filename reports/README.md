# Release Reports

This directory stores generated release artefacts such as walk-forward regression summaries and canary deployment reports. Files under `reports/releases/` are created during CI and are ignored from source control; they are uploaded as workflow artefacts for traceability.
